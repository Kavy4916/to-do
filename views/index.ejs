<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do</title>
</head>
<body>
<%-include("partitions/header.ejs") %>
<div class="container">
<% if(locals.list) { %>
<%  for(let i=0; i<list.length; i++){ %>
    <div class="list-item"><input type="checkbox"><%=list[i]%></input></div>
    <% }} %>
<div class="append">
    <form action="/add" method="post">
        <input type="text" name="element"  placeholder="Add Item" required>
        <input type="submit" value="Add"> 
    </form>
</div>
</div>

<%-include("partitions/footer.ejs") %>
</body>
</html>